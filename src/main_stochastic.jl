# do like main.jl but by running stochastic simulation for one DEM.

# load surface and bedrock for 201,204

# create the + and - bedrock from 1) gpr +- 2) glate unc (see emirical law). Compute the corr length (use xdem?)

# create the = and - for surface. compute corr length. 

# define error in flotation factor + corr length.

# run WWFS stochastic 
# TODO: add lake analyse INSIDE WWFS so the output is a spread for each paramters (3: zs, zb and f)

#Plot this nicely: boxplot ? boxplot for 4 case: the total, and the 3 contribution. Which is the largest ?